<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.CommentMapper">

    <sql id="Base_Column_List">
        id,content,user_id,pid,time,fid,module,root_id
    </sql>

    <select id="selectAll" resultType="com.example.entity.Comment">
        select comment.*, user.name as userName
        from comment
        left join user on comment.user_id = user.id
        <where>
            <if test="content != null"> and comment.content like concat('%', #{content}, '%')</if>
        </where>
        order by comment.id desc
    </select>

    <select id="selectById" resultType="com.example.entity.Comment">
        select
        <include refid="Base_Column_List" />
        from comment
        where id = #{id}
    </select>

    <delete id="deleteById">
        delete from comment
        where  id = #{id}
    </delete>

    <insert id="insert" parameterType="com.example.entity.Comment" useGeneratedKeys="true" keyProperty="id">
        insert into comment
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != null">id,</if>
            <if test="content != null">content,</if>
            <if test="userId != null">user_id,</if>
            <if test="pid != null">pid,</if>
            <if test="time != null">time,</if>
            <if test="fid != null">fid,</if>
            <if test="module != null">module,</if>
            <if test="rootId != null">root_id,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id != null">#{id},</if>
            <if test="content != null">#{content},</if>
            <if test="userId != null">#{userId},</if>
            <if test="pid != null">#{pid},</if>
            <if test="time != null">#{time},</if>
            <if test="fid != null">#{fid},</if>
            <if test="module != null">#{module},</if>
            <if test="rootId != null">#{rootId},</if>
        </trim>
    </insert>

    <update id="updateById" parameterType="com.example.entity.Comment">
        update comment
        <set>
            <if test="content != null">
                content = #{content},
            </if>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="pid != null">
                pid = #{pid},
            </if>
            <if test="time != null">
                time = #{time},
            </if>
            <if test="fid != null">
                fid = #{fid},
            </if>
            <if test="module != null">
                module = #{module},
            </if>
            <if test="rootId != null">
                root_id = #{rootId},
            </if>
        </set>
        where id = #{id}
    </update>

    <select id="selectRoot" resultType="com.example.entity.Comment">
        select c.*, u.name as userName, u.avatar as userAvatar
        from comment c
        left join user u on u.id = c.user_id
        where pid is null and c.fid = #{fid} and c.module = #{module}
        order by c.time desc
    </select>

    <select id="selectByRootId" resultType="com.example.entity.Comment">
        select c1.*, u1.name as userName, u1.avatar as userAvatar, u2.name as parentName
        from comment c1
        left join user u1 on u1.id = c1.user_id
        left join comment c2 on c1.pid = c2.id
        left join user u2 on c2.user_id = u2.id
        where c1.pid is not null and c1.root_id = #{rootId}
        order by c1.time desc
    </select>

    <select id="selectCount" resultType="int">
        select count(*) from comment where fid = #{fid} and module = #{module}
    </select>
</mapper>